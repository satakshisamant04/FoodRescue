# PORT is fixed to 3000 by platform
# MONGODB_URI: Optional MongoDB Connection URI if connecting to remote cluster
MONGODB_URI=
APP_URL=

# AI Chatbot & RAG Configuration (Server-Side)
GEMINI_API_KEY=
TOP_K=4
VECTOR_DB_TYPE=local
